[package]
name = "agile_config"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.5"
thiserror = "1.0"

[dev-dependencies]
pretty_assertions = "1.4"
