[package]
name = "agile_store_redis"
version = "0.1.0"
edition = "2021"

[dependencies]
redis = { version = "0.23", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
agile_common = { path = "../common" }

[dev-dependencies]
pretty_assertions = "1.4"
